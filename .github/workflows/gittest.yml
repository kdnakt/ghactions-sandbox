name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: update hoge.txt
      run: |
        echo date > hoge.txt
        git add .
        git commit -m "update hoge.txt"
        git push
        echo finish!
